platform :ios, '8.0'
use_frameworks!
podspec :path => 'Ably.podspec'

def test_pods
    pod 'Quick', '1.2.0'
    pod 'Nimble', '7.0.3'
    # Helpers
    pod 'Aspects'
    pod 'SwiftyJSON', '3.1.4'
end

target 'AblySpec' do
    test_pods
end
