on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    uses: ably/ably-cocoa/.github/workflows/integration-test.yaml@feature/1273-one-job-per-workflow
    with:
      platform: macOS
      lane: test_macOS
