name: "Integration Test: tvOS"

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    uses: ably/ably-cocoa/.github/workflows/integration-test.yaml
    with:
      lane: test_tvOS12
