name: "Integration Test: iOS 12"

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    uses: ./.github/workflows/integration-test.yaml@feature/1273-one-job-per-workflow
    with:
      lane: test_iOS12
