name: "Integration Test"

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

# IMPORTANT NOTES:
# - The Fastlane lane name is duplicated in more than one place within this workflow.

permissions:
  contents: read

jobs:
  check:
    runs-on: macos-15

    # These Fastlane lanes build the `ably-cocoa` scheme against
    # Test/Ably.xctestplan, which runs the AblyTests, AblyTestsObjC and UTS
    # targets. This is the ONLY place CI executes the `UTS` target's tests
    # (the Universal Test Suite at Test/UTS, derived from the
    # ably/specification `uts/` specs) — including the LiveObjects `objects`
    # unit tier at Test/UTS/unit/objects, the sandbox-network integration
    # tier, and (macOS only) the proxy tier. liveobjects.yaml deliberately
    # has no UTS job (see the note in that file); its SPM job still compiles
    # the UTS target under -warnings-as-errors. The lanes raise the iOS/tvOS
    # test deployment targets in fastlane/Fastfile so that UTS code linking
    # AblyLiveObjects (iOS 14 / tvOS 14 floor) compiles.
    strategy:
      fail-fast: false
      matrix:
        include:

          - platform: iOS
            lane: test_iOS26_2

          - platform: tvOS
            lane: test_tvOS26_2

          - platform: macOS
            lane: test_macOS

    env:
      LC_CTYPE: en_US.UTF-8
      LANG: en_US.UTF-8
      ABLY_ENV: sandbox

    steps:
      - name: Check out repo
        uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2.7.0
        with:
          persist-credentials: false

      - name: Select Xcode (16.4)
        run: |
          sudo xcode-select -s /Applications/Xcode_16.4.app
          echo "Selected Xcode version:"
          xcodebuild -version

      - name: Environment Info
        run: ./Scripts/log-environment-information.sh

      - name: Check out xcparse repo
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          repository: ably-forks/xcparse
          ref: emit-test-case-info
          path: xcparse
          persist-credentials: false

      - id: get-xcparse-commit-sha
        name: Get xcparse commit SHA
        run: |
          cd xcparse
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: "actions/cache (xcparse binary)"
        uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3.5.0
        with:
          path: xcparse/.build/debug/xcparse
          key: ${{ runner.os }}-xcparse-${{ steps.get-xcparse-commit-sha.outputs.sha }}

      - name: Reset Simulators
        run: xcrun simctl erase all

      - name: Install Dependencies
        run: |
          brew install xcbeautify
          make submodules
          bundle install

      - name: Run Tests
        run: bundle exec fastlane ${{ matrix.lane }}

      - name: Check Static Analyzer Output
        id: analyzer-output
        run: |
          if [[ -z $(find ./derived_data -name "report-*.html") ]]; then
            echo "Static Analyzer found no issues."
          else
            echo "Static Analyzer found some issues. HTML report will be available in Artifacts section. Failing build."
            exit 1
          fi
      
      - name: Upload Static Analyzer Reports
        if: ${{ failure() && steps.analyzer-output.outcome == 'failure' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: static-analyzer-reports-${{ matrix.lane }}
          path: ./derived_data/**/report-*.html
      
      - name: Upload Xcodebuild Logs
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: xcodebuild-logs-${{ matrix.lane }}
          path: ~/Library/Developer/Xcode/DerivedData/*/Logs

      - name: Upload Test Output
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: test-output-${{ matrix.lane }}
          path: fastlane/test_output

      - name: Upload Test Results
        if: always()
        env:
          TEST_OBSERVABILITY_SERVER_AUTH_KEY: ${{ secrets.TEST_OBSERVABILITY_SERVER_AUTH_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
           Scripts/upload_test_results.sh --job-name "check (${{ matrix.platform }}, ${{ matrix.lane }})"

