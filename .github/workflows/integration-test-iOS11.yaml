on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    uses: ./.github/workflows/integration-test.yaml@feature/1273-one-job-per-workflow
    with:
      platform: iOS
      lane: test_iOS11
